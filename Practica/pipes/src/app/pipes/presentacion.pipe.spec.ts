import { PresentacionPipe } from './presentacion.pipe';

describe('PresentacionPipe', () => {
  it('create an instance', () => {
    const pipe = new PresentacionPipe();
    expect(pipe).toBeTruthy();
  });
});
