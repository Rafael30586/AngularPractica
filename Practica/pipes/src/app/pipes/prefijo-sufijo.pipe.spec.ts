import { PrefijoSufijoPipe } from './prefijo-sufijo.pipe';

describe('PrefijoSufijoPipe', () => {
  it('create an instance', () => {
    const pipe = new PrefijoSufijoPipe();
    expect(pipe).toBeTruthy();
  });
});
