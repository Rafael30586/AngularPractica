import { CambiarLetrasPipe } from './cambiar-letras.pipe';

describe('CambiarLetrasPipe', () => {
  it('create an instance', () => {
    const pipe = new CambiarLetrasPipe();
    expect(pipe).toBeTruthy();
  });
});
