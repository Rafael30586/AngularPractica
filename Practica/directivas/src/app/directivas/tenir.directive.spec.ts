import { TenirDirective } from './tenir.directive';

describe('TenirDirective', () => {
  it('should create an instance', () => {
    const directive = new TenirDirective();
    expect(directive).toBeTruthy();
  });
});
