import { Tenir2Directive } from './tenir2.directive';

describe('Tenir2Directive', () => {
  it('should create an instance', () => {
    const directive = new Tenir2Directive();
    expect(directive).toBeTruthy();
  });
});
