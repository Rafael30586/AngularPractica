import { PonerFotoDirective } from './poner-foto.directive';

describe('PonerFotoDirective', () => {
  it('should create an instance', () => {
    const directive = new PonerFotoDirective();
    expect(directive).toBeTruthy();
  });
});
