import { TenirLetrasDirective } from './tenir-letras.directive';

describe('TenirLetrasDirective', () => {
  it('should create an instance', () => {
    const directive = new TenirLetrasDirective();
    expect(directive).toBeTruthy();
  });
});
