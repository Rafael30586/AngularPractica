import { DarClaseDirective } from './dar-clase.directive';

describe('DarClaseDirective', () => {
  it('should create an instance', () => {
    const directive = new DarClaseDirective();
    expect(directive).toBeTruthy();
  });
});
